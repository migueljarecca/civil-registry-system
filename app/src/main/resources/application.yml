spring:
  application:
    name: civil-registry-db
  profiles:
    active:
      - prod

---
spring:
  config:
    activate:
      on-profile: prod
    #import: optional:file:.env[.properties]
  datasource:
    url: jdbc:postgresql://db.xfaclyojrilwzwgekcxh.supabase.co:5432/postgres
    username: postgres
    password: F6xgfFoMyboMb08f
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
    show-sql: true
